const items = document.querySelectorAll("[data-item='item']");
const countNumbers = document.querySelectorAll("[data-number='count']");
const textMessages = document.querySelectorAll(".message__text");
const ballsRed = document.createElement("span");
ballsRed.appendChild(ballsRed);

// textMessages.forEach((text) => {
//   text.appendChild(ballsRed);
// });

window.addEventListener("DOMContentLoaded", () => {
  //   textMessages.forEach((text) => {
  //     ballsRed.appendChild(text);
  //     // console.log(text);
  //   });
  //   ballsRed.forEach((ball) => {
  // ball.classList.toggle("show");
  //   });
});
